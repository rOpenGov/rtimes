#' @param ... Curl options (debugging tools mostly) passed to \code{\link[httr]{GET}}
#' @param key (character) your Propublica API key; pass in, or loads from .Renviron
#' as \code{PROPUBLICA_API_KEY}. See \code{\link{rtimes-package}} for info on rate limits.
